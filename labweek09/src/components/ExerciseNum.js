import React, {Component} from "react";

export default class ExerciseNum extends Component{
    render(){
        return(
            <h2>React JS Progamming {this.props.work}</h2>
        )
    }
}
